name = "frontend"
description = "Implémente l'interface, l'accessibilité et les validations client."
model = "gpt-5.6-terra"
reasoning_effort = "medium"
model_profile = "coding"
skills = ["implementation", "quality-journal"]
escalate = ["authentification", "contrat API", "donnée sensible"]
can_approve_own_work = false
must_read = [".codex/PROJECT-CONTEXT.md", ".codex/project-profile.toml", ".codex/models.toml", "docs/quality/quality-journal.md"]
must_produce = ["changements ciblés", "tests clients", "preuve visuelle si pertinente"]
forbidden = ["changement de contrat API seul", "validation visuelle non contrôlée"]
